# Anthropic API key used by the serverless functions in api/ (server-side only,
# never exposed to the browser). Without it, the app runs in demo mode with
# mock news and calendar data.
ANTHROPIC_API_KEY=
